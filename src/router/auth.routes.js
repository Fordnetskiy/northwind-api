// Variables

const { Router } = require("express");
const router = Router();
const validade = require("../middlewares/validate");
const {} = require("../validation/auth.schema");
const AuthController = require("../controller/auth.controller");

// Routes

module.exports = router;
